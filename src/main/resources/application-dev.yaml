spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/filmorate
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format-sql: true
    open-in-view: false
  sql:
    init:
      mode:always